# NTR `golang` 分支审查与修复报告

## 审查范围

本次审查基于仓库 `https://github.com/moeart/ntr` 的 `golang` 分支，历史基线提交为 `53c6050`（`5.3b`），最终发布版本为 `6.0.2026.0820`。审查覆盖 TTL 探测调度、ICMP 回包解析、终端刷新、统计并发、配置消费、错误处理，以及 Windows 构建和 TUI 交互。[1]

## 主要问题与根因

| 问题 | 根因 | 影响 |
| --- | --- | --- |
| 终端闪屏、撕裂或标题与数据行不同步 | 旧实现混用 goterm 缓冲、标准输出和逐行光标控制，并可能对每个回包立即刷新 | 半帧显示、行错位、重复标题、旧行残留和明显闪烁 |
| `127.0.0.1` 看起来比公网目标快很多 | 本机目标通常在 TTL 1 直接返回；旧调度仍按极短默认周期和高频回包刷新，导致本地反馈非常密集 | 用户观察到本机探测速度异常，且容易误以为 TTL 请求失控 |
| TTL 请求产生突发 | 每轮同时启动全部 TTL 的 goroutine；`hop_sleep` 没有真正控制默认调度 | raw socket 和 ICMP 请求在短时间内集中产生，网络负载和调度抖动明显 |
| 配置文件大部分不生效 | 配置虽然加载，但 timeout、interval、max_hops、display、hop_sleep、ASN/GeoIP 开关没有完整传入运行时 | 修改 `config.yaml` 后行为仍由 CLI 全局默认值主导 |
| ICMP 权限或 socket 错误导致进程 panic | `ListenPacket` 失败时调用 `log.Panicf` | 无 raw-socket 权限时进程或探测 goroutine 直接崩溃 |
| ICMP 解析存在异常输入风险 | 读取错误、超时、IPv4/IPv6 嵌套数据、类型断言和切片边界缺少统一保护 | 畸形或截断回包可能触发 panic、忙等或错误匹配 |
| 渲染热路径重复查询 | 每次渲染同步执行 ASN、GeoIP 和可选 PTR 查询 | 探测越快，重复查询越多，造成 UI 卡顿、抖动和输入延迟 |
| 无意义的高 TTL 探测 | 目标已在较低 TTL 返回后，后续轮次仍固定探测到 `max_hops` | 额外产生大量请求，放大速度异常 |
| QQWry 依赖服务器时区 | 是否使用 QQWry 由主机时区字符串推断 | UTC 等服务器会意外关闭配置中启用的 QQWry |
| 小屏界面过度占用空间 | 头部固定 4 行、底部固定 2 行、10 列完整表格始终显示 | 80x24 等小终端出现横向拥挤和纵向浪费 |
| `127.0.0.1` 重复显示最后一跳 | 目标 TTL 收缩后旧的高 TTL 统计仍保留在快照显示范围内 | 本机目标会显示多行相同地址，造成“重复最后一跳” |
| 状态栏为空 | 旧底部 TextView 只有标题，没有连接到探测计数和生命周期 | 用户无法知道轮次、发包数、回包数或最近活动 |
| 第三方终端残影/错位 | Unicode 线框、非默认背景色和增量绘制对不同终端的支持不一致 | Kommander 等终端出现叠字、旧内容残留或边框错位 |

## `8.8.8.8` 与 `127.0.0.1` 复现结论

在修改前使用相同参数运行两个目标，`127.0.0.1` 在 5 秒转录中产生 25 个 UI 帧，而 `8.8.8.8` 产生 15 个 UI 帧；本机目标的探测结果更快完成，因此旧 UI 更容易暴露高频刷新问题。这个差异本身并不表示本机网络延迟异常，而是目标在 TTL 1 返回后，程序没有把“目标 TTL 很低”和“轮询周期”明确分离。

修复后默认 `interval` 从 `200ms` 调整为 `1s`，示例配置和 Windows 发布配置均同步更新。探测器收到目标回包后会把下一轮上限收缩到目标 TTL，同时快照只展示当前 active TTL 范围，因此 `127.0.0.1` 默认每轮只发起 TTL 1 探测且只显示一行；`8.8.8.8` 则根据实际路径保留必要的 TTL 范围。显式传入 `-i` 仍可覆盖默认周期，例如 `-i 500ms`，这属于用户主动选择的频率。

在新版伪终端转录中，两个目标均能够进入全屏 TUI；新 UI 以约 250ms 的固定轮询频率更新，3 秒测试中约产生 13 个标题更新，不再随每个 TTL 回包刷新。80x24 小终端会自动切换为 6 列紧凑表格，120 列以内使用中等列集，宽屏才显示完整 10 列。当前沙箱没有可用于抓包的 `strace`/`tcpdump`，因此未声称完成系统级 ICMP 包计数；调度代码和可停止生命周期已通过单元、竞态、构建及伪终端启动验证。

## 已实施修复

### 探测调度与生命周期

TTL 调度不再在一个瞬间并发启动全部 hop。`hop_sleep` 大于零时按配置使用；为避免默认值为零时重新形成突发，系统会在一个 `interval` 周期内均匀分布各 TTL，最小间隔为 1ms。每轮探测不会与下一轮重叠，`interval` 表示轮次起始时间之间的最小周期。目标主机返回后，下一轮自动将 TTL 上限收缩到目标 TTL；如果目标暂时消失，则根据 `max_unknown_hops` 逐步扩大探测范围。

NTR 增加 `context` 驱动的 `Stop` 生命周期，退出 TUI 时会停止发现循环，不再依赖 `os.Exit` 强行结束后台 goroutine。计时等待使用可取消 timer，退出时不会继续无意义地等待完整 interval。

### ICMP 错误和解析

ICMP 失败现在返回带上下文的 error，不再调用 `log.Panicf`。IPv4 和 IPv6 读取循环明确设置读截止时间，所有非成功读取错误直接返回，嵌套 Time Exceeded 数据在解析前进行长度和类型校验，Echo Reply 同时校验 ID、序列号和 payload。这样可以把权限、网络和畸形回包问题转化为可统计的失败探测，而不是进程级崩溃。

### 统计并发与查询缓存

每个 `HopStatistic` 增加专用读写锁；探测结果写入、旧渲染接口和新 TUI 快照不会互相读写未同步字段。TUI 快照只在 NTR 锁内快速收集 hop 指针，随后在锁外完成显示数据组装，避免慢 DNS、ASN 或 GeoIP 查询阻塞探测结果注册。ASN、GeoIP 和 PTR 结果复用每跳缓存，减少刷新热路径中的重复外部查询。

### TUI 重构

CLI 已从手写 raw mode、stdin 监听、光标移动、清屏和刷新 goroutine 切换到成熟的 `github.com/rivo/tview` `v0.42.0`，底层使用 `github.com/gdamore/tcell/v2`。视觉层恢复用户指定的原始 NTR 表格：顶部居中产品标题和版权信息，下一行显示 DEST/START，中间为无大块边框的紧凑路由表，底部显示实时状态与按键提示。

本轮进一步增加了 `activeHops` 生命周期，快照只输出当前有效 TTL，解决 `127.0.0.1` 的重复最后一跳；空目标且 100% 丢包的行显示为“请求超时”。界面保持用户指定的原始 NTR 表格风格：顶部为居中的产品标题和版权信息，下一行显示 `DEST` 与 `START`，中央为原始十列紧凑表格，底部显示运行状态和按键提示。小终端不再擅自删除 ASN、平均延迟或位置列，而是按原始格式压缩每列宽度；IPv4/IPv6 目标仍会动态调整目标列宽。

TUI 已恢复中英文界面：`-L zh` 使用中文列名和状态，`-L en` 使用 `HOST`、`LOSS%`、`SENT`、`LAST`、`BEST`、`AVG`、`WORST`、`IP LOCATION` 等英文文本。表头不再统一居中，而是复用对应数据列的 alignment：`#` 居中，目标/主机/ASN/位置左对齐，丢包、发送和延迟等数字列右对齐，确保标题行与数据行严格对齐。

为兼容 Kommander 等第三方终端，TUI 的普通标题、DEST/START、正文、状态和快捷键区域恢复使用终端自身的默认前景色和背景色，不修改用户的 ConEmu 配色；表头仅在自身单元格上使用局部黑字白底样式，等价复现原始 render.go 的 `Color(title, BLACK)` + `Background(..., WHITE)` 语义，不改变普通区域或终端全局配色。表格本身不再绘制密集 Unicode 内部分隔线，并在每次绘制前清空 screen，避免缩短内容后留下叠字和残影。鼠标输入已关闭，不会因指针移动或点击改变画面；Hop 不再有选中行、粗体或 Selected 状态。键盘 `↑` / `↓` 被映射为整页 PageUp / PageDown 翻页，按 `c` 可开关丢包颜色，按 `q`、`Esc` 或 `Ctrl+C` 退出，按 `r` 刷新。Hop 超过可视高度时，右侧动态显示 ASCII 滚动轨道和滑块；未溢出时不占用表格宽度。footer 使用 ASCII `[Q]uit [R]efresh [C]olor [UP/DN]Page` 和 `[Q]退出 [R]刷新 [C]颜色 [UP/DN]翻页`，避免 ConEmu 对 Unicode 箭头宽度的异常处理。

| 操作 | 行为 |
| --- | --- |
| 鼠标输入 | 已关闭，不改变 Hop 或界面状态 |
| `↑` / `↓` | 整页向上/向下翻页，跟随右侧滚动条 |
| `c` | 开关丢包颜色 |
| `r` | 立即刷新 |
| `q` / `Esc` / `Ctrl+C` | 退出并停止后台探测 |

采用 tview 而不是继续扩展手写 ANSI 的原因是它直接提供 `Application`、`Table`、`Flex`、`Box`、边框、标题、输入捕获和安全的 `QueueUpdateDraw`，更适合当前实时路由表场景。[2][3]

### 配置和构建

配置中的 timeout、interval、display、语言、PTR、环形缓冲区以及 ASN/GeoIP 开关已接入运行时，并保留命令行参数优先级。QQWry 选择改为配置驱动，仅在英文界面主动关闭。默认周期、README、中文 README 和 Windows 发布目录中的 `config.yaml` 均已统一为 `1s`。

## 验证结果

| 验证项 | 结果 |
| --- | --- |
| `go test -count=1 ./...` | 通过 |
| `go test -race -count=1 ./...` | 通过 |
| `go vet ./...` | 通过 |
| `git diff --check` | 通过 |
| Linux 项目构建 | 通过 |
| Windows `amd64` 交叉编译 | 通过，PE32+ x86-64 |
| Windows `386` 交叉编译 | 通过，PE32 Intel 80386 |
| Windows `arm64` 交叉编译 | 通过，PE32+ AArch64 |
| 无 ICMP raw-socket 权限启动 | 通过，不再 panic |
| 伪终端启动 `8.8.8.8` | 通过，能进入 tview 全屏界面 | 
| 104x30 / xterm-256color | 通过，原始风格标题、DEST/START、紧凑表格和底部提示均可见 |
| `80x24 / xterm-256color` | 通过，原始十列、ASN/位置列、底部提示和 ASCII 兼容输出 |
| `80x24 / vt100` | 通过，无 Unicode 线框残影 |
| 官方 ConEmu v23.07.24 + Wine/Xvfb/x11vnc | 通过，VNC 截图确认普通区域沿用终端默认配色，tview 表头局部使用原始黑字白底语义，ASCII footer、原始十列和右侧滚动条正常 |
| ConEmu 连续窗口 resize | 通过，1000x600、760x420、1180x700、900x520、1280x800 连续调整后进程保持运行，无卡死 |
| 伪终端启动 `127.0.0.1` | 通过，active TTL 为 1，只显示有效最后一跳 |
| 超时跳显示 | 通过，空目标且 100% 丢包显示“请求超时” |
| `-L zh` / `-L en` | 通过，中文/英文列名、状态和超时文案均切换 |
| 表头与数据对齐 | 通过，列标题复用数据列的 left/right/center alignment |
| Xvfb + Openbox + x11vnc 图形终端 | 通过，普通区域不覆盖终端主题，表头局部黑字白底和底部提示完整 |
| Kommander 浅色桌面回归样式 | 通过，普通区域沿用用户终端配色，表头局部黑字白底，footerHelp 按文本实际宽度右对齐并完整显示最近活动时间 |
| Hop 溢出可视区域 | 通过，右侧显示 ASCII `|` 轨道和 `#` 滑块，UP/DN 整页翻页后随 Table offset 移动 |

`go mod tidy` 在当前受限环境中还会尝试下载某些依赖包的测试依赖（例如 `golang.org/x/tools`），因此本次验证采用离线缓存完成的 `go test`、`go vet`、竞态检查和构建；项目源码本身已通过这些验证。真实公网 IPv4/IPv6 Time Exceeded 回包仍建议在具备 raw-socket 权限的 Linux、macOS 或 Windows 环境中做一次端到端测试。

## 交付内容

压缩包包含修改后的工作树、Windows 三架构 exe、配置、启动器、回归测试和本报告。仓库未提交新的 Git commit，原始基线仍可通过 `git diff` 或压缩包内的工作树复核。

## References

[1]: https://github.com/moeart/ntr/tree/golang "moeart/ntr：golang 分支"

[2]: https://github.com/rivo/tview "rivo/tview 官方仓库"

[3]: https://pkg.go.dev/github.com/rivo/tview "tview 官方 Go 文档"
